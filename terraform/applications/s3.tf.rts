/*
resource "aws_s3_bucket" "logs" {
  bucket_prefix = "${local.app_env}-${var.app_parent}-${var.app_name}-logs-"
  acl    = "log-delivery-write"

  tags = "${merge(
    local.common_tags,
    map(
      "Name", "${local.app_env}-${var.app_parent}-${var.app_name}-logs"
    )
  )}"
}
*/

